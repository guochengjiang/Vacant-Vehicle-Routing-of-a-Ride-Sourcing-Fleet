[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fleet-routing-research"
version = "0.3.0"
requires-python = ">=3.10"
dependencies = ["numpy", "scipy", "pandas", "psutil", "matplotlib"]

[tool.setuptools.packages.find]
where = ["src"]
